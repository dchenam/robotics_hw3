cmake_minimum_required(VERSION 2.8)
project( cam_calibration )
find_package( OpenCV REQUIRED )
add_executable( cam_calibration cam_calibration.cpp )
target_link_libraries( cam_calibration ${OpenCV_LIBS} )
